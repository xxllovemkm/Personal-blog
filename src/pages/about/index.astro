---
import Page from '../../layouts/Page.astro';
import { Image } from 'astro:assets';
import avatar from '../../assets/images/avatar.webp';
import { openReviewProfile, publications } from '../../data/publications';
---

<Page title="关于 - 许兴龙">
  <div class="grid md:grid-cols-[180px_1fr] gap-8 items-start">
    <aside class="card text-center">
      <div class="w-28 h-28 mx-auto mb-4 rounded-full overflow-hidden border border-smoke dark:border-dark-border bg-smoke dark:bg-dark-border">
        <Image
          src={avatar}
          alt="许兴龙"
          width={224}
          height={224}
          class="w-full h-full object-cover"
        />
      </div>
      <h1 class="text-xl font-heading mb-2">许兴龙</h1>
      <p class="text-sm text-ink-light dark:text-dark-text-secondary">
        AI Agent / 数据工程 / 科学文档智能
      </p>
    </aside>

    <div class="max-w-prose space-y-8 text-ink dark:text-dark-text">
      <section class="space-y-4">
        <h2 class="text-lg font-heading">关于我</h2>
        <p>
          我是许兴龙，中国科学院大学计算机技术硕士，研究和工程兴趣集中在 AI Agent、面向大模型的数据工程、多模态推理以及科学文档智能。
        </p>
        <p>
          近期工作包括几何生成式推理评测、可测试可诊断的数据工程范式，以及面向 LaTeX 论文的 vision-in-the-loop 视觉排版优化系统。
        </p>
      </section>

      <section class="space-y-3">
        <h2 class="text-lg font-heading">教育背景</h2>
        <div class="space-y-2 text-sm">
          <p>2019 - 2023：山东科技大学，计算机科学与技术，本科</p>
          <p>2023 - 2025：中国科学院大学，计算机技术，硕士研究生</p>
        </div>
      </section>

      <section class="space-y-3">
        <h2 class="text-lg font-heading">科研方向</h2>
        <ul class="list-disc pl-5 space-y-2 text-sm">
          <li>多模态模型的几何生成式推理与可验证评测。</li>
          <li>面向大模型的 test-driven data engineering 与诊断驱动数据补强。</li>
          <li>科学文档的视觉排版优化、LaTeX 编译反馈与多 Agent 闭环系统。</li>
        </ul>
      </section>

      <section class="space-y-3">
        <h2 class="text-lg font-heading">代表工作</h2>
        <div class="space-y-4 text-sm">
          {publications.map((paper) => (
            <article>
              <div class="flex flex-wrap items-center gap-2 mb-1 text-xs text-ink-light dark:text-dark-text-secondary">
                <span class="tag text-xs">{paper.venue} {paper.year}</span>
                <span>{paper.role}</span>
                <span>{paper.status}</span>
              </div>
              <h3 class="font-heading">{paper.title}</h3>
              <p class="mt-1 text-ink-light dark:text-dark-text-secondary">
                {paper.summary}
              </p>
              <div class="flex flex-wrap gap-3 mt-2">
                {paper.links.openreview && (
                  <a href={paper.links.openreview} class="text-xs no-underline">OpenReview</a>
                )}
                {paper.links.arxiv && (
                  <a href={paper.links.arxiv} class="text-xs no-underline">arXiv</a>
                )}
                {paper.links.project && (
                  <a href={paper.links.project} class="text-xs no-underline">Project</a>
                )}
              </div>
            </article>
          ))}
        </div>
      </section>

      <section class="space-y-3">
        <h2 class="text-lg font-heading">联系方式</h2>
        <ul class="space-y-1 text-sm">
          <li>GitHub：<a href="https://github.com/xxllovemkm">@xxllovemkm</a></li>
          <li>OpenReview：<a href={openReviewProfile}>Xinglong Xu</a></li>
          <li>邮箱：<a href="mailto:xuxinglong00423@163.com">xuxinglong00423@163.com</a></li>
        </ul>
      </section>
    </div>
  </div>
</Page>
